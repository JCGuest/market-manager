<br><br>
<div class="header-row" id="header-row" style="padding: 0px; overflow:hidden; height:60px; background-color: white; margin:0 auto; width:70%;">
    <h2 align="center" >New Order</h2>
</div> 
<div style="width:72%; margin:0 auto;">
    <h3>
        <small class='text-muted'>
        <%if !flash[:message].blank? %>
        <%= flash[:message] %>
        <% end %>

        <% if !@order.errors.full_messages.blank?  %>
        <% @order.errors.full_messages.each do |message| %>
        <%=message %>
        <br>
        <% end %>
        <% end %>

        
        <% if @patron && !@patron.errors.full_messages.blank?  %>
        <% @patron.errors.full_messages.each do |message| %>
        Patron <%= message %>
        <br>
        <% end %>
        <%end %>
    </h3>
</div>


<br>

<%= render partial: 'form', locals: { order: @order, clerk: @clerk, patron:(:patron_attributes), patrons: Patron.all.reverse, url: clerk_orders_url } %>

